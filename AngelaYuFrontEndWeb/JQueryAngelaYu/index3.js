$('h1').text("Bye"); // The .text() method edits the text inside the selected element but not the html
$('button').text("Don't Click me!");

// The .html() method edits the text and the html depending on the tags. uncomment them below
// $("h1").html("<strong>What are we doing today!</strong>");
// $("button").html("<em>Hey</em>");

// // NB: If it was the .text() method It will give an output with the tags involved
// $("h1").text("<strong>What are we doing today!</strong>")
// $("button").text("<em>Hey</em>");
